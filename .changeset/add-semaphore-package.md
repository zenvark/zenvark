---
"@zenvark/semaphore": minor
"@zenvark/prom": minor
---

Add `@zenvark/semaphore` — an adaptive distributed semaphore coordinated via Redis, with AIMD capacity control, priority classes and crash-safe leases — and `PrometheusSemaphoreMetrics` in `@zenvark/prom` for its observability.
