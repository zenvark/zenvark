packages:
  - "apps/*"
  - "packages/*"

allowBuilds:
  core-js: false
  core-js-pure: false
  esbuild: true

peerDependencyRules:
  allowedVersions:
    # redis-semaphore declares ioredis@^4.1.0 || ^5; zenvark is verified against ioredis 6.
    "redis-semaphore>ioredis": "6"
